enabled: true

ingresses:
  - name: argo
    ssl: true
    rules:
      # TODO: change host after upgrade to RKE2 is complete
      - serviceName: argo-server
        servicePort: 2746

secrets:
  - name: argo-workflows-sso
    data:
      client-id: "~vault:Projects/k8s/argo-sso:client-id~"
      client-secret: "~vault:Projects/k8s/argo-sso:client-secret~"
