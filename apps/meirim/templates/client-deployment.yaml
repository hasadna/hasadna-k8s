apiVersion: apps/v1
kind: Deployment
metadata:
  name: client
spec:
  selector:
    matchLabels:
      app: client
  replicas: 2
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
      - name: client
        image: ghcr.io/hasadna/meirim/meirim-client:f3e4f7140920dc6a9fa96718a7ec6794be51f720
        resources:
          requests:
            cpu: "250m"
            memory: "500Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
