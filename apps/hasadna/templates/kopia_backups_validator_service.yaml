{{ if (gt (atoi $.Capabilities.KubeVersion.Minor) 30) }}
apiVersion: v1
kind: Service
metadata:
  name: kopia-backups-validator
spec:
  selector:
    app: kopia-backups-validator
  ports:
  - port: 80
{{ end }}
