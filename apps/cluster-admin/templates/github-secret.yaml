{{ if .Values.githubSecretEnabled }}
apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: github
  namespace: default
data:
  .dockerconfigjson: "~vault:Projects/k8s/dockerconfig:json~"
{{ end }}
