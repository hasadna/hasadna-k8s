apiVersion: v1
kind: Service
metadata:
  name: jenkins
  namespace: cattle-monitoring-system
  labels:
    name: jenkins
spec:
  ports:
  - port: 9796
    targetPort: 9796
  type: ExternalName
  externalName: ~iac:jenkins_ip~
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: jenkins
  namespace: cattle-monitoring-system
spec:
  selector:
    matchLabels:
      name: jenkins
  endpoints:
  - port: "9796"
