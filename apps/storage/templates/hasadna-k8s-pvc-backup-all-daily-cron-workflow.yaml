apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: hasadna-k8s-pvc-backup-all-daily
spec:
  schedule: "47 1 * * *"
  suspend: true  # temporary, until I fix the backup of large data to be more efficient, should be done tomorrow
  concurrencyPolicy: Forbid
  workflowSpec:
    workflowTemplateRef:
      name: hasadna-k8s-pvc-backup-all
