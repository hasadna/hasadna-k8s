apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: hasadna
spec:
  imageCatalogRef:
    apiGroup: postgresql.cnpg.io
    kind: ImageCatalog
    name: hasadna
    major: 18
  instances: 1
  storage:
    pvcTemplate:
      storageClassName: ""
      volumeMode: Filesystem
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 5Gi
      selector:
        matchLabels:
          app.kubernetes.io/name: hasadna-pv-cloudnative-pg-hasadna-pg-cluster
          app.kubernetes.io/managed-by: terraform-hasadna-rke2-storage
  enableSuperuserAccess: true
  superuserSecret:
    name: hasadna-postgres-superuser
  monitoring:
    enablePodMonitor: true
