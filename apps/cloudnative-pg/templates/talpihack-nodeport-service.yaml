apiVersion: v1
kind: Service
metadata:
  name: talpihack-nodeport
spec:
  type: NodePort
  selector:
    cnpg.io/cluster: talpihack
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 32719
