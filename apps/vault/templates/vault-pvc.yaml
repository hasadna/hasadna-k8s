apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      app.kubernetes.io/name: vault
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: vault
  labels:
    app.kubernetes.io/name: vault
spec:
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - worker1
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 10Gi
  local:
    path: /srv/storage/infra/vault
