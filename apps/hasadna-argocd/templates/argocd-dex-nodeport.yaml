apiVersion: v1
kind: Service
metadata:
  name: argocd-dex-nodeport
  namespace: argocd
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: argocd-dex-server
  ports:
    - name: "5556"
      port: 5556
      nodePort: 30635
